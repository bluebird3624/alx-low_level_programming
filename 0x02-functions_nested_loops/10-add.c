#include "main.h"
/**
 * add - takes in two numbers and adds them together returning the result
 * Return: the sumation results
 * @a: the first integer place holder
 * @b: the second integer place holder
 */

int add(int a, int b)
{
	int res;

	res = a + b;

	return (res);
}
